#!/usr/bin/env bash
# Quick launcher for Review Anchor (using venv)
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VENV_DIR="$DIR/.venv"

if [ ! -d "$VENV_DIR" ]; then
    python3 -m venv "$VENV_DIR"
fi

exec "$VENV_DIR/bin/python" "$DIR/review-anchor/review_anchor.py" "$@"
